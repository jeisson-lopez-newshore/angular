export class Prueba {
    blVariable: boolean;
    strVariable: string;
    numVariable: number;

    constructor(blVariable: boolean, strVariable: string, numVariable: number) {
        this.blVariable = blVariable;
        this.strVariable = strVariable;
        this.numVariable = numVariable;
    }
}
